import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'stirling Arms Hotel',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
